{
  "name": "foundry-backend",
  "version": "1.0.0",
  "description": "Proxy backend that streams catalog files from GitHub to the Foundry frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
